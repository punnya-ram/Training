server:
  port: 8887
  
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/punnya-ram/spring-cloud-config-server.git
          search-paths:
          - '{application}'
     
management:
  endpoints:
    web:
      exposure:
        include:
        - "*"     
   